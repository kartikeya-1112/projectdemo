pipeline{
    stages{
stage('Print PATH') 
        {
            steps {
                script {
                    echo "Current PATH: ${env.PATH}"
                }
            }
        }
    }
}